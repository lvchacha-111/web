# XinWeb 3D - 防打垮部署指南

## 🎯 已实施的防打垮措施

### **1. 服务器端优化**
- ✅ **Gzip压缩**：减少70%网络流量
- ✅ **智能缓存**：静态文件缓存7天
- ✅ **IP限流**：1000请求/15分钟/每个IP
- ✅ **下载限速**：大文件500KB/秒
- ✅ **内存监控**：自动清理防泄漏
- ✅ **健康检查**：`GET /health` 监控接口

### **2. 文件说明**
- `server-防打垮.js` - 优化后的主服务器
- 替换原来的 `server.js` 即可

## 🚀 阿里云部署步骤

### **第一步：服务器准备**
```bash
# 1. 购买阿里云ECS
# 推荐配置：2核4G，5M带宽，Ubuntu 22.04

# 2. 连接服务器
ssh root@你的服务器IP

# 3. 基础环境
apt update && apt upgrade -y
apt install -y nodejs npm nginx git
```

### **第二步：部署应用**
```bash
# 1. 创建项目目录
mkdir -p /var/www/xinweb
cd /var/www/xinweb

# 2. 上传代码（使用FTP或Git）
# 将D:\xinweb\所有文件上传到此目录

# 3. 安装依赖
npm install

# 4. 修改.env文件（重要！）
nano .env
# 修改密码为强密码
ADMIN_PASSWORD=你的强密码_这里修改
```

### **第三步：配置Nginx（关键！）**
```nginx
# /etc/nginx/sites-available/xinweb
server {
    listen 80;
    server_name 你的域名.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name 你的域名.com;
    
    # SSL证书（Let's Encrypt免费）
    ssl_certificate /etc/letsencrypt/live/你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/你的域名.com/privkey.pem;
    
    # 安全头部
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    
    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
    
    # 反向代理到Node.js
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 静态文件直接由Nginx服务（性能关键！）
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|ttf|woff)$ {
        root /var/www/xinweb;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 管理后台保护（只允许你的IP）
    location /admin {
        allow 你的公网IP;  # 修改为你的IP
        deny all;
        proxy_pass http://localhost:3000;
    }
}
```

### **第四步：进程管理**
```bash
# 1. 安装PM2（进程管理）
npm install -g pm2

# 2. 启动应用
cd /var/www/xinweb
pm2 start server-防打垮.js --name xinweb
pm2 start admin.js --name xinweb-admin

# 3. 设置开机自启
pm2 startup
pm2 save
```

### **第五步：防火墙配置**
```bash
# 阿里云安全组设置（控制台操作）
开放端口：
- 80 (HTTP)
- 443 (HTTPS) 
- 22 (SSH，限制你的IP)

关闭端口：
- 3000 (管理后台，用Nginx代理)
- 5000 (主服务，用Nginx代理)
```

## 📊 监控与维护

### **日常监控命令**
```bash
# 查看应用状态
pm2 status

# 查看日志
pm2 logs xinweb

# 健康检查
curl http://localhost:5000/health

# 服务器状态
top -u www-data  # 查看进程资源
df -h            # 磁盘空间
free -h          # 内存使用
```

### **自动备份脚本**
```bash
# 创建备份脚本 /root/backup-xinweb.sh
#!/bin/bash
BACKUP_DIR="/backups/xinweb"
DATE=$(date +%Y%m%d_%H%M%S)

# 备份代码
tar -czf $BACKUP_DIR/code_$DATE.tar.gz /var/www/xinweb

# 备份数据库（如果有）
# mysqldump -u root -p database > $BACKUP_DIR/db_$DATE.sql

# 保留最近7天备份
find $BACKUP_DIR -type f -mtime +7 -delete

# 添加到crontab每天凌晨3点执行
# crontab -e
# 0 3 * * * /root/backup-xinweb.sh
```

## 🛡️ 应急处理

### **如果网站被打垮**
```bash
# 1. 查看攻击来源
tail -f /var/log/nginx/access.log | grep -v "你的IP"

# 2. 临时封禁IP
iptables -A INPUT -s 攻击者IP -j DROP

# 3. 启用更严格限流（临时）
# 修改server-防打垮.js中的MAX_REQUESTS_PER_IP为100
# 然后重启：pm2 restart xinweb

# 4. 启用阿里云DDoS防护（控制台）
```

### **如果服务器内存不足**
```bash
# 1. 查看内存使用
pm2 monit

# 2. 重启应用释放内存
pm2 restart xinweb

# 3. 优化大文件
# 压缩字体文件：simhei.ttf (9.7MB) → 使用系统字体
```

## 💡 性能优化建议

### **立即实施**
1. **启用CDN** - 阿里云CDN加速静态文件
2. **图片压缩** - 压缩所有图片到合适大小
3. **字体优化** - 使用WOFF2格式或系统字体

### **中期优化**
1. **数据库优化** - 如果添加用户系统
2. **Redis缓存** - 缓存频繁访问的数据
3. **负载均衡** - 流量大时添加多台服务器

### **高级防护**
1. **WAF防火墙** - 阿里云Web应用防火墙
2. **DDoS高防** - 如果经常被攻击
3. **安全审计** - 定期扫描漏洞

## 📞 故障排查

### **网站无法访问**
```bash
# 1. 检查服务状态
systemctl status nginx
pm2 status

# 2. 检查端口
netstat -tulpn | grep :5000
netstat -tulpn | grep :80

# 3. 检查日志
tail -100 /var/log/nginx/error.log
pm2 logs xinweb --lines 100
```

### **管理后台无法登录**
1. 检查`.env`文件中的密码
2. 检查Nginx IP白名单配置
3. 检查admin.js是否正常运行

## ✅ 上线检查清单

- [ ] 修改了`.env`中的默认密码
- [ ] 配置了Nginx SSL证书
- [ ] 设置了阿里云安全组
- [ ] 配置了PM2开机自启
- [ ] 测试了健康检查接口
- [ ] 备份了初始数据
- [ ] 记录了服务器IP和密码

---

**最后提醒**：定期更新系统、备份数据、监控日志。  
遇到问题先检查日志，大多数问题都有错误信息。  
祝你的3D网站顺利上线！ 🚀