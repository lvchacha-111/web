# 🚀 XinWeb 3D 平台 - 阿里云上线清单

**项目**：XinWeb 3D 产品展示平台  
**负责人**：梓瑞  
**技术协助**：绿茶茶 🍵  
**创建时间**：2026-03-25  
**预计总耗时**：2-4 小时（分阶段完成）

---

## 📋 清单使用说明

- **标记**：
  - [梓瑞] 👤 - 需要你执行的操作
  - [绿茶茶] 🤖 - 我帮你完成的操作
  - [合作] 👥 - 我们一起完成
- **优先级**：
  - 🔴 高优先级（必须完成）
  - 🟡 中优先级（建议完成）
  - 🟢 低优先级（可选优化）

---

## 🏗️ 第一阶段：本地准备（30分钟）

### 🔴 安全加固
- [ ] [梓瑞] **修改管理后台密码**
  - 打开 `D:\xinweb\.env` 文件
  - 将 `ADMIN_PASSWORD=11223344` 改为强密码
  - 建议格式：`大小写字母+数字+符号`，如 `XinWeb_2026@Secure`
  - 保存文件

- [ ] [梓瑞] **测试管理后台登录**
  ```bash
  cd D:\xinweb
  node admin.js
  ```
  - 浏览器访问 `http://localhost:3000`
  - 使用新密码登录
  - 确认功能正常

### 🔴 代码清理
- [ ] [绿茶茶] **敏感信息检查**
  ```bash
  # 检查硬编码的密码、密钥
  grep -r "password\|secret\|key\|token" . --include="*.js" --include="*.json"
  ```

- [ ] [合作] **确认.gitignore配置**
  - 确认包含：`.env`, `node_modules/`, `backups/`, `*.log`
  - 避免上传敏感文件

### 🟡 文档准备
- [ ] [绿茶茶] **整理压力测试报告**
  - 将报告内容通过飞书发给你
  - 你粘贴到新建的飞书文档

- [ ] [绿茶茶] **创建部署脚本**
  - 编写 `deploy.sh`（阿里云用）
  - 编写 `start.sh`（本地测试用）

---

## 🌐 第二阶段：阿里云购买与配置（60分钟）

### 🔴 服务器购买
- [ ] [梓瑞] **购买阿里云 ECS**
  - 地域：华东1（杭州）或华北2（北京）
  - 实例：共享标准型 s6（1核2G）足够
  - 系统：Ubuntu 22.04 LTS 或 CentOS 7.9
  - 带宽：按流量计费，1-2Mbps 起步
  - 存储：系统盘 40GB，数据盘可选

- [ ] [梓瑞] **获取服务器信息**
  - 公网IP：`xxx.xxx.xxx.xxx`
  - 登录密码：记录好
  - 实例ID：备用

### 🔴 安全组配置
- [ ] [梓瑞] **配置防火墙规则**
  ```
  必需开放：
  - 22端口：SSH（所有IP或仅你的IP）
  - 80端口：HTTP（未来Nginx用）
  - 443端口：HTTPS（未来SSL用）
  - 5000端口：主网站（临时，后期改Nginx）
  
  限制访问：
  - 3000端口：管理后台（仅你的IP访问）
  ```

- [ ] [梓瑞] **设置SSH密钥对**（可选但推荐）
  - 创建SSH密钥对
  - 下载私钥到本地
  - 配置到ECS实例

### 🟡 域名准备（可选）
- [ ] [梓瑞] **域名购买与解析**
  - 购买域名（阿里云万网）
  - 添加A记录到ECS公网IP
  - 解析生效时间约10分钟

---

## 📤 第三阶段：上传与部署（60分钟）

### 🔴 服务器环境
- [ ] [绿茶茶] **提供环境安装命令**
  ```bash
  # Ubuntu/Debian
  curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
  sudo apt update && sudo apt install -y nodejs git nginx
  npm install -g pm2
  
  # CentOS/RHEL
  curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
  sudo yum install -y nodejs git nginx
  npm install -g pm2
  ```

- [ ] [梓瑞] **远程连接服务器**
  ```bash
  # Windows 使用 PowerShell 或 PuTTY
  ssh root@你的公网IP
  # 输入密码
  ```

- [ ] [合作] **创建专用用户**
  ```bash
  # 在服务器上执行
  useradd -m -s /bin/bash xinweb
  passwd xinweb
  # 设置密码
  
  # 切换到新用户
  su - xinweb
  ```

### 🔴 代码上传
- [ ] [梓瑞] **上传代码到服务器**
  ```bash
  # 本地压缩代码（排除不需要的文件）
  cd D:\xinweb
  # 使用 WinSCP 或 FileZilla 上传
  
  # 或者使用 git（如果配置了仓库）
  git clone 你的仓库地址
  ```

- [ ] [合作] **设置目录权限**
  ```bash
  # 在服务器上
  cd /home/xinweb/app
  chmod 755 .
  chmod -R 755 uploads images stl
  ```

### 🔴 环境配置
- [ ] [梓瑞] **创建服务器上的 .env 文件**
  ```bash
  cd /home/xinweb/app
  nano .env
  # 粘贴以下内容（修改密码）：
  PORT=5000
  ADMIN_PORT=3000
  ADMIN_USER=admin
  ADMIN_PASSWORD=你的强密码_这里修改
  SECRET_KEY=生成一个随机字符串
  BACKUP_ENABLED=true
  BACKUP_DIR=./backups
  BACKUP_RETENTION_DAYS=7
  BACKUP_SOURCES=uploads,images,stl
  ```

- [ ] [绿茶茶] **提供部署脚本**
  ```bash
  # deploy.sh 内容
  #!/bin/bash
  cd /home/xinweb/app
  npm install --production
  pm2 start server.js --name "xinweb-main"
  pm2 start admin.js --name "xinweb-admin"
  pm2 save
  pm2 startup
  ```

---

## 🧪 第四阶段：测试与安全（30分钟）

### 🔴 服务启动测试
- [ ] [梓瑞] **启动服务**
  ```bash
  cd /home/xinweb/app
  node server.js &
  node admin.js &
  
  # 或用PM2
  pm2 start server.js --name "xinweb-main"
  pm2 start admin.js --name "xinweb-admin"
  ```

- [ ] [梓瑞] **端口访问测试**
  ```
  测试以下地址：
  1. http://你的公网IP:5000 - 主网站（应该能访问）
  2. http://你的公网IP:3000 - 管理后台（应该要求登录）
  3. 使用新密码登录管理后台
  4. 测试上传、删除功能
  ```

### 🟡 备份系统测试
- [ ] [合作] **测试备份脚本**
  ```bash
  cd /home/xinweb/app
  node backup.js --dry-run
  node backup.js
  
  # 设置定时任务
  crontab -e
  # 添加：0 2 * * * cd /home/xinweb/app && node backup.js >> backup.log 2>&1
  ```

### 🟡 性能监控
- [ ] [绿茶茶] **提供监控命令**
  ```bash
  # 查看进程状态
  pm2 status
  pm2 logs xinweb-main
  pm2 logs xinweb-admin
  
  # 查看资源使用
  top
  free -h
  df -h
  ```

---

## 🛡️ 第五阶段：高级安全（可选，30分钟）

### 🟡 Nginx + HTTPS
- [ ] [绿茶茶] **提供Nginx配置**
  ```nginx
  # /etc/nginx/sites-available/xinweb
  server {
      listen 80;
      server_name 你的域名.com;
      return 301 https://$server_name$request_uri;
  }
  
  server {
      listen 443 ssl;
      server_name 你的域名.com;
      
      ssl_certificate /etc/letsencrypt/live/域名/fullchain.pem;
      ssl_certificate_key /etc/letsencrypt/live/域名/privkey.pem;
      
      location / {
          proxy_pass http://localhost:5000;
          proxy_set_header Host $host;
      }
  }
  ```

- [ ] [梓瑞] **申请SSL证书**
  ```bash
  # 使用 Let's Encrypt
  sudo apt install certbot python3-certbot-nginx
  sudo certbot --nginx -d 你的域名.com
  ```

### 🟡 防火墙加固
- [ ] [绿茶茶] **提供iptables规则**
  ```bash
  # 只开放必要端口
  iptables -A INPUT -p tcp --dport 22 -j ACCEPT
  iptables -A INPUT -p tcp --dport 80 -j ACCEPT
  iptables -A INPUT -p tcp --dport 443 -j ACCEPT
  iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
  iptables -A INPUT -j DROP
  ```

### 🟡 入侵检测
- [ ] [绿茶茶] **提供日志监控脚本**
  ```bash
  # 监控失败登录
  tail -f /var/log/auth.log | grep "Failed password"
  
  # 安装fail2ban
  sudo apt install fail2ban
  sudo systemctl enable fail2ban
  ```

---

## 🔄 第六阶段：维护与监控（持续）

### 🟢 日常维护
- [ ] [合作] **定期更新系统**
  ```bash
  # 每周执行
  sudo apt update && sudo apt upgrade -y
  npm update
  ```

- [ ] [梓瑞] **监控磁盘空间**
  ```bash
  # 检查备份目录大小
  du -sh /home/xinweb/app/backups/
  
  # 清理旧日志
  pm2 flush
  ```

- [ ] [绿茶茶] **提供健康检查脚本**
  ```bash
  # health-check.sh
  #!/bin/bash
  # 检查服务状态、磁盘、内存、备份
  ```

### 🟢 备份验证
- [ ] [梓瑞] **每月测试恢复**
  ```bash
  # 从备份恢复一个测试文件
  cp /home/xinweb/app/backups/最新备份/images/测试图片.jpg /tmp/
  # 验证文件完整
  ```

---

## 🚨 紧急情况处理

### 网站无法访问
1. **检查服务状态**：`pm2 status`
2. **查看错误日志**：`pm2 logs xinweb-main --error`
3. **重启服务**：`pm2 restart xinweb-main`
4. **检查端口**：`netstat -tlnp | grep :5000`

### 管理后台无法登录
1. **检查admin服务**：`pm2 logs xinweb-admin`
2. **验证.env配置**：`cat /home/xinweb/app/.env`
3. **重启admin**：`pm2 restart xinweb-admin`

### 磁盘空间不足
1. **清理旧备份**：`node backup.js --retention 3`（保留3天）
2. **清理npm缓存**：`npm cache clean --force`
3. **清理pm2日志**：`pm2 flush`

---

## 📞 技术支持

### 联系信息
- **项目**：XinWeb 3D 平台
- **负责人**：梓瑞
- **技术支持**：绿茶茶 🍵（通过此会话）
- **阿里云工单**：控制台提交

### 关键信息记录
- **服务器IP**：__________________
- **SSH端口**：22（默认）
- **管理后台密码**：__________________
- **域名**：__________________（如有）
- **备份目录**：`/home/xinweb/app/backups/`

---

## ✅ 完成标准

1. [ ] 主网站可通过 `http://IP:5000` 访问
2. [ ] 管理后台可通过 `http://IP:3000` 登录（仅你的IP）
3. [ ] 上传、删除功能正常
4. [ ] 备份脚本每天自动运行
5. [ ] PM2 设置开机自启
6. [ ] 所有密码均为强密码
7. [ ] 安全组配置正确

---

**最后更新**：2026-03-25  
**版本**：1.0  
**下次复查**：上线后一周